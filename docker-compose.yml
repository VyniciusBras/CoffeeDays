services:
  db:
    image: postgres
    environment:
      POSTGRES_DB: db-coffeedays
      POSTGRES_USER: user-coffeedays
      POSTGRES_PASSWORD: password
    volumes:
      - cofeedays-data:/var/lib/postgresql/data
    ports:
      - "5432:5432"

volumes:
  cofeedays-data:
